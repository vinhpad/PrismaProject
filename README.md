# PrismaProject
- Pre process
  >   nano .env
  > 
  >   DATABASE_URL = "mysql://{USER}:{PASSWORD}@{HOST}:{PORT}/{DATABASE_NAME}"
  > 
- Run project
  > npm install
  > 
  > prisma generate 
  >
  > node index.js 